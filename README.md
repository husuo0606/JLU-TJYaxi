Since GitHub does not allow single files to exceed 25MB, the observation files for a single set of data were split into several parts.

I have integrated all the ephemeris data from the same system into a single file. Since these ephemeris data are only used to calculate satellite positions and not for positioning, they do not contain ionospheric parameters.
